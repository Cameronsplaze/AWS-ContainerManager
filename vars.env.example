export AWS_PROFILE=<profile>
export AWS_REGION=<region>

### For minecraft example specifically
export RCRON_PASSWORD=<minecraft-rcron-pass>

### TMP for lambda development
export ECS_CLUSTER_NAME=<cluster-name>
export ECS_CLUSTER_SERVICE=<cluster-service>
export ASG_NAME=<asg-name>
export TASK_DEFINITION=<task-definition>

### To only declare in one spot. Will be in config when split happens.
# export DOCKER_IMAGE="nginx:latest"
# export DOCKER_PORT=80
export DOCKER_IMAGE="itzg/minecraft-server"
export DOCKER_PORT=25565
